(setq mivector (make-array 34))
(setq a 0)


(defun main() 
	(setq b 34)
	(setf (aref mivector 5) 15)
	(setq c 0)
	(setq a 8)
	(setq a 8)
(loop while ( < a  200 ) do (setq b ( - b  1 ))
	(print a)
	 (setq a ( + a  1 )))
	(setq b ( * (aref mivector 5)  b ))
	(print "Resultado final:")
	(print b)
	(return-from main 0)
	
) (main)
