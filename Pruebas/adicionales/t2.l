(setq a 0)
(setq b 0)
(setq c 0)


(defun main() 
	(setq d 0)
	(setq a 9)
	(print "a vale ahora")
	(print a)
	(return-from main a)
	(setq b 56)
	(print "b vale ahora")
	(print b)
	(return-from main b)
	(setq c 12)
	(print "c vale ahora")
	(print c)
	(return-from main c)
	(setq d ( + ( + a  b )  c ))
	(print "suma total")
	(print d)
	(return-from main d)
	
) (main)
